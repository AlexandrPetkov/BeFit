1) Доделать redirect. Проблема: если мы переходим на страницу через form в параметрах реквеста идет /controller и все.
	Варианты решения:
		а) кидать в сессию, если сессия закончилась, кидать на index;
		b) кидать в реквест, но тогда нужно делать это везде
	Проблема: ни в сессию ни в реквест не пойдет страница по которой перешли вне контроллера. Т.е. надо запрашивать реквест/сессию и если они равны null, то брать referer 